import { EventBlockDirective } from './event-block.directive';

describe('EventBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new EventBlockDirective();
    expect(directive).toBeTruthy();
  });
});
